apiversion: v1
kind: Pod
metadata:
  name: secret-pod
  spec:
    containers:
      - name: secret-pod
        image: nginx
        ports:
          - containerPort: 80
        env:
          -secretRef: 
              name: devops-secret
